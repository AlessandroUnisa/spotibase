
<div>
    <button class="artista">
        <a href="${param.prefix}artista?nomeDArte=${param.nomeDArte}">
            <img src="${param.srcImg}" class="${param.classImg}">
        </a>
        </button>
    <div class="centro">
        ${param.nomeDArte}
    </div>
</div>